# TINT - Интернет-магазин корейской косметики

Современный интернет-магазин корейской косметики с полным функционалом электронной коммерции.

## 🚀 Живая демонстрация

👉 **[Посмотреть демо](https://korean-cosmetics-tint.vercel.app/)**

### Демо-учетные данные

#### 👤 Пользователь
*Роль: user (нет доступа к админ-панели)*

```
Email:    demo-user@tint.com
Password: DemoUserPass1234-
```

#### 🔐 Администратор
*Роль: admin (полный доступ к админ-панели)*

```
Email:    demo-admin@tint.com
Password: DemoAdminPass1234-
```

> ⚠️ **Примечание:** Демо-аккаунты предназначены только для тестирования.

## 📋 Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Функциональность](#функциональность)
- [Установка](#установка)
- [Настройка](#настройка)
- [Запуск проекта](#запуск-проекта)
- [Структура проекта](#структура-проекта)
- [API](#api)
- [Разработка](#разработка)

## 📖 Описание

TINT - это полнофункциональный интернет-магазин корейской косметики. Проект включает в себя фронтенд на React и бэкенд на Node.js с Express.

### Основные возможности:

- 🛍️ Каталог товаров с фильтрацией по категориям и брендам
- 🔍 Поиск товаров
- 🛒 Корзина покупок (для авторизованных и гостевых пользователей)
- 👤 Система аутентификации и авторизации с ролями (user, manager, admin)
- 📦 Оформление заказа
- 📝 Блог о корейской косметике
- 🛡️ Админ панель с защитой доступа на основе ролей
- 📊 Статистика продаж для администраторов
- 🎛️ Управление товарами и заказами
- 📱 Адаптивный дизайн
- ⚡ Оптимизация производительности (lazy loading, пагинация)

## 🛠 Технологии

### Frontend:
- **React 18** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **Redux Toolkit** - управление состоянием
- **React Router** - маршрутизация
- **Tailwind CSS** - стилизация
- **Swiper** - слайдеры
- **Firebase** - аутентификация

### Backend:
- **Node.js** - серверная платформа
- **Express 5** - веб-фреймворк
- **PostgreSQL** - база данных
- **Firebase Admin SDK** - серверная аутентификация

### Инструменты разработки:
- **ESLint** - линтер кода
- **Prettier** - форматирование кода
- **Nodemon** - автоперезагрузка сервера
- **Concurrently** - параллельный запуск frontend и backend

## ✨ Функциональность

### Для пользователей:
- Просмотр каталога товаров
- Фильтрация по категориям и брендам
- Поиск товаров
- Просмотр детальной информации о товаре
- Добавление товаров в корзину
- Управление количеством товаров в корзине, очистка корзины
- Регистрация и авторизация
- Оформление заказа
- Просмотр истории заказов
- Чтение блога о косметике

### Для администраторов и менеджеров:
- 📊 **Статистика** (только admin) - просмотр популярных товаров за последние 12 месяцев
- 📦 **Управление заказами** (admin, manager) - просмотр и изменение статусов заказов
- 🛒 **Управление товарами** (только admin) - добавление, редактирование и деактивация товаров
- 🔐 **Защита доступа** - система ролей с проверкой прав на бэкенде и фронтенде

## 🚀 Установка

### Требования:
- Node.js (версия 20 или выше)
- PostgreSQL
- npm или yarn

### Шаги установки:

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/saiaana/korean-cosmetics-TINT
cd korean-cosmetics-TINT
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте базу данных:**
   - Создайте базу данных PostgreSQL
   - Выполните миграции (если есть)

## ⚙️ Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта и файл `backend/.env` для бэкенда.

#### Frontend (.env в корне):
```env
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_firebase_measurement_id
VITE_API_BASE_URL=http://localhost:4000
```

#### Backend (backend/.env):
```env
PG_HOST=localhost
PG_PORT=5432
PG_USER=your_postgres_user
PG_PASSWORD=your_postgres_password
PG_DATABASE=your_database_name

```

### Firebase Admin SDK

Поместите файл сервисного аккаунта Firebase в `backend/config/` или настройте использование переменных окружения.

## 🏃 Запуск проекта

### Режим разработки:

Запуск frontend и backend одновременно:
```bash
npm run dev
```

Или отдельно:
```bash
# Frontend (порт 5174)
npm run dev-frontend

# Backend (порт 4000)
npm run dev-backend
```

### Production сборка:

1. **Соберите frontend:**
```bash
npm run build
```

2. **Запустите backend:**
```bash
npm run dev-backend
# или используйте PM2 для production
```

### Миграции базы данных:
```bash
npm run migrate
```

## Scripts

- upload-to-cloudinary.js — массовая загрузка изображений
- update-catalog-images.js — обновление catalog_images.url
- update-variant-images.js — обновление variant_images.url

## 📁 Структура проекта

```
korean-cosmetics-TINT/
├── backend/                 # Backend приложение
│   ├── config/             # Конфигурационные файлы
│   ├── constants/           # Константы
│   ├── controllers/         # Контроллеры (обработка запросов)
│   ├── middleware/          # Middleware (аутентификация, проверка ролей)
│   ├── repositories/        # Репозитории (работа с БД)
│   ├── routes/              # Маршруты API
│   ├── scripts/             # Скрипты (миграции, синхронизация)
│   ├── services/            # Бизнес-логика
│   ├── db.js               # Подключение к БД
│   ├── firebaseAdmin.js    # Инициализация Firebase Admin
│   └── index.js            # Точка входа сервера
│
├── src/                     # Frontend приложение
│   ├── api/                 # API клиенты
│   ├── assets/              # Статические ресурсы
│   ├── auth/                # Логика аутентификации
│   ├── components/          # React компоненты
│   │   ├── common/          # Общие компоненты (ProtectedRoute и т.д.)
│   │   ├── layout/         # Компоненты макета
│   │   ├── pages/          # Компоненты страниц
│   │   │   └── admin/      # Админские компоненты (таблицы)
│   │   └── ui/             # UI компоненты
│   ├── config/              # Конфигурация
│   ├── constants/           # Константы (роли, админское меню)
│   ├── data/                # Статические данные
│   ├── features/            # Feature-модули (FSD)
│   ├── hooks/               # Custom hooks (useAdminOrder, useAdminProducts, useAdminStats)
│   ├── pages/               # Страницы приложения
│   ├── store/               # Redux store
│   ├── utils/               # Утилиты
│   ├── App.jsx              # Главный компонент
│   └── main.jsx             # Точка входа
│
├── public/                   # Публичные файлы
├── package.json              # Зависимости и скрипты
├── vite.config.js           # Конфигурация Vite
└── tailwind.config.js       # Конфигурация Tailwind
```

## 🔌 API

### Основные эндпоинты:

#### Товары:
- `GET /api/products/categoriesList` - Список категорий
- `GET /api/products/categories/:category` - Товары по категории
- `GET /api/products/brands` - Список брендов
- `GET /api/products/brands/:brand` - Товары по бренду
- `GET /api/products/new` - Новые товары
- `GET /api/products/on-sale` - Товары со скидкой
- `GET /api/products/bestsellers` - Хиты продаж
- `GET /api/products/slug/:slug` - Товар по slug
- `GET /api/products/search` - Поиск товаров

#### Корзина:
- `GET /api/cart` - Получить корзину
- `POST /api/cart` - Добавить товар в корзину
- `PUT /api/cart/:productId` - Обновить товар в корзине
- `DELETE /api/cart/:productId` - Удалить товар из корзины
- `DELETE /api/cart` - Очистить корзину
- `POST /api/cart/merge` - Объединить гостевую корзину с пользовательской

#### Заказы:
- `POST /api/orders` - Создать заказ (требует аутентификации)
- `GET /api/orders/user/:firebaseUid` - Заказы пользователя
- `GET /api/orders/:orderId` - Детали заказа

#### Админ API (требует специальных ролей):
- `GET /api/orders/all` - Все заказы (admin, manager)
- `PUT /api/orders/:orderId/status` - Обновить статус заказа (admin, manager)
- `GET /api/stats/popular-products` - Популярные товары (только admin)
- `GET /api/products/admin/all` - Все товары для админки (только admin)
- `GET /api/products/admin/:productId` - Товар для редактирования (только admin)
- `POST /api/products` - Создать товар (только admin)
- `PUT /api/products/:productId` - Обновить товар (только admin)
- `PUT /api/products/:productId/active` - Изменить статус активности (только admin)

#### Пользователи:
- `POST /api/users` - Создать пользователя
- `GET /api/users/me` - Текущий пользователь (требует аутентификации, возвращает роль)

## 💻 Разработка

### Архитектура:

Проект использует **Feature-Sliced Design** подход для организации кода:
- `features/` - изолированные фичи (cart, products)
- `components/` - переиспользуемые компоненты
- `pages/` - страницы приложения
- `hooks/` - кастомные хуки (включая админские: `useAdminOrder`, `useAdminProducts`, `useAdminStats`)
- `utils/` - утилиты
- `constants/` - константы (роли, админское меню)

**Паттерн разделения компонентов:**
Админские страницы следуют паттерну разделения логики и представления:
- `AdminPage.jsx` (страница) → `useAdminHook.js` (логика) + `AdminTable.jsx` (представление)

### State Management:

- **Redux Toolkit** для глобального состояния
- **createEntityAdapter** для нормализации данных
- **createSelector** для мемоизации селекторов

### Стилизация:

- **Tailwind CSS** для стилей
- Компоненты используют группированные стили в объектах для удобства поддержки
- Адаптивный дизайн с breakpoints: sm, md, lg, xl

### Оптимизация:

- **Lazy loading** изображений через Intersection Observer
- **Infinite scroll** для пагинации товаров
- **Code splitting** через React.lazy()
- **Skeleton screens** для улучшения UX
- **Мемоизация** компонентов и селекторов

### Линтинг и форматирование:

```bash
# Проверка кода
npm run lint

# Форматирование
npm run format
npm run format:check
```

## 📝 Скрипты

- `npm run dev` - Запуск frontend и backend в режиме разработки
- `npm run dev-frontend` - Запуск только frontend
- `npm run dev-backend` - Запуск только backend
- `npm run build` - Сборка frontend для production
- `npm run preview` - Предпросмотр production сборки
- `npm run lint` - Проверка кода линтером
- `npm run format` - Форматирование кода через Prettier
- `npm run format:check` - Проверка форматирования без изменений
- `npm run migrate` - Выполнение миграций БД

## 🔒 Безопасность

- **Аутентификация** через Firebase
- **JWT токены** для API запросов
- **Система ролей** (user, manager, admin) с проверкой прав доступа
- **Middleware проверки ролей** на бэкенде для всех админских endpoints
- **Защита роутов** на фронтенде через компонент `ProtectedRoute`
- **CORS настройки** для защиты API
- **Валидация входных данных** на уровне контроллеров и сервисов
- **Защита от SQL инъекций** через параметризованные запросы
- **Транзакции** для обеспечения целостности данных

### Роли и права доступа:

- **user** (по умолчанию) - обычный пользователь, может делать заказы
- **manager** - может управлять заказами (просмотр, изменение статусов)
- **admin** - полный доступ: управление товарами, заказами, просмотр статистики

**Важно:** Реальная защита происходит на бэкенде. Фронтенд используется только для UX (скрытие/показ элементов интерфейса).

## 📚 Документация

Подробная документация находится в папке `docs/`:

- **[auth.md](docs/auth.md)** - Аутентификация, авторизация и система ролей
- **[api.md](docs/api.md)** - Полная документация API endpoints
- **[architecture.md](docs/architecture.md)** - Архитектура проекта
- **[database.md](docs/database.md)** - Структура базы данных

## 👥 Автор

Pet project - TINT Korean Cosmetics Shop

---

**Примечание:** Для работы проекта необходимо настроить все переменные окружения и базу данных согласно разделу [Настройка](#настройка). Для доступа к админ панели необходимо создать пользователя с ролью `admin` или `manager` в базе данных.

## 🌐 Другие языки

- [English](README.md)
- [한국어 (Korean)](README.kr.md)
