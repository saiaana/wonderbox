# TINT - 한국 화장품 전자상거래 플랫폼

완전한 전자상거래 기능을 갖춘 현대적인 한국 화장품 전자상거래 플랫폼입니다.

## 🚀 라이브 데모

👉 **[데모 보기](https://korean-cosmetics-tint.vercel.app/)**

### 데모 자격 증명

#### 👤 사용자 계정
*역할: user (관리자 패널 접근 불가)*

```
Email:    demo-user@tint.com
Password: DemoUserPass1234-
```

#### 🔐 관리자 계정
*역할: admin (관리자 패널 전체 접근 가능)*

```
Email:    demo-admin@tint.com
Password: DemoAdminPass1234-
```

> ⚠️ **참고:** 데모 계정은 테스트 목적으로만 사용됩니다.

## 📋 목차

- [설명](#설명)
- [기술 스택](#기술-스택)
- [기능](#기능)
- [설치](#설치)
- [설정](#설정)
- [프로젝트 실행](#프로젝트-실행)
- [프로젝트 구조](#프로젝트-구조)
- [API](#api)
- [개발](#개발)

## 📖 설명

TINT는 완전한 기능을 갖춘 한국 화장품 전자상거래 플랫폼입니다. 이 프로젝트는 React 프론트엔드와 Express를 사용한 Node.js 백엔드를 포함합니다.

### 주요 기능:

- 🛍️ 카테고리 및 브랜드별 필터링이 가능한 제품 카탈로그
- 🔍 제품 검색
- 🛒 쇼핑 카트 (인증된 사용자 및 게스트 사용자용)
- 👤 역할 기반 인증 및 권한 시스템 (user, manager, admin)
- 📦 주문하기
- 📝 한국 화장품 블로그
- 🛡️ 역할 기반 접근 제어가 있는 관리자 패널
- 📊 관리자용 판매 통계
- 🎛️ 제품 및 주문 관리
- 📱 반응형 디자인
- ⚡ 성능 최적화 (지연 로딩, 페이지네이션)

## 🛠 기술 스택

### Frontend:
- **React 18** - UI 라이브러리
- **Vite** - 빌드 도구 및 개발 서버
- **Redux Toolkit** - 상태 관리
- **React Router** - 라우팅
- **Tailwind CSS** - 스타일링
- **Swiper** - 슬라이더
- **Firebase** - 인증

### Backend:
- **Node.js** - 서버 플랫폼
- **Express 5** - 웹 프레임워크
- **PostgreSQL** - 데이터베이스
- **Firebase Admin SDK** - 서버 측 인증

### 개발 도구:
- **ESLint** - 코드 린터
- **Prettier** - 코드 포맷터
- **Nodemon** - 서버 자동 재시작
- **Concurrently** - 프론트엔드와 백엔드 병렬 실행

## ✨ 기능

### 사용자용:
- 제품 카탈로그 탐색
- 카테고리 및 브랜드별 필터링
- 제품 검색
- 제품 상세 정보 보기
- 장바구니에 제품 추가
- 장바구니 항목 수량 관리, 장바구니 비우기
- 회원가입 및 인증
- 주문하기
- 주문 내역 보기
- 화장품 블로그 읽기

### 관리자 및 매니저용:
- 📊 **통계** (admin 전용) - 지난 12개월 인기 제품 보기
- 📦 **주문 관리** (admin, manager) - 주문 보기 및 상태 업데이트
- 🛒 **제품 관리** (admin 전용) - 제품 추가, 편집 및 비활성화
- 🔐 **접근 제어** - 백엔드 및 프론트엔드에서 권한 확인이 있는 역할 기반 시스템

## 🚀 설치

### 요구사항:
- Node.js (버전 20 이상)
- PostgreSQL
- npm 또는 yarn

### 설치 단계:

1. **저장소 클론:**
```bash
git clone https://github.com/saiaana/korean-cosmetics-TINT
cd korean-cosmetics-TINT
```

2. **의존성 설치:**
```bash
npm install
```

3. **데이터베이스 설정:**
   - PostgreSQL 데이터베이스 생성
   - 마이그레이션 실행 (있는 경우)

## ⚙️ 설정

### 환경 변수

프로젝트 루트에 `.env` 파일과 백엔드용 `backend/.env` 파일을 생성하세요.

#### Frontend (루트의 .env):
```env
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id
VITE_FIREBASE_APP_ID=your_firebase_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_firebase_measurement_id
VITE_API_BASE_URL=http://localhost:4000
```

#### Backend (backend/.env):
```env
PG_HOST=localhost
PG_PORT=5432
PG_USER=your_postgres_user
PG_PASSWORD=your_postgres_password
PG_DATABASE=your_database_name

```

### Firebase Admin SDK

Firebase 서비스 계정 파일을 `backend/config/`에 배치하거나 환경 변수 사용을 구성하세요.

## 🏃 프로젝트 실행

### 개발 모드:

프론트엔드와 백엔드를 동시에 실행:
```bash
npm run dev
```

또는 별도로:
```bash
# Frontend (포트 5174)
npm run dev-frontend

# Backend (포트 4000)
npm run dev-backend
```

### 프로덕션 빌드:

1. **프론트엔드 빌드:**
```bash
npm run build
```

2. **백엔드 실행:**
```bash
npm run dev-backend
# 또는 프로덕션용 PM2 사용
```

### 데이터베이스 마이그레이션:
```bash
npm run migrate
```

## Scripts

- upload-to-cloudinary.js — 이미지 대량 업로드
- update-catalog-images.js — catalog_images.url 업데이트
- update-variant-images.js — variant_images.url 업데이트

## 📁 프로젝트 구조

```
korean-cosmetics-TINT/
├── backend/                 # 백엔드 애플리케이션
│   ├── config/             # 설정 파일
│   ├── constants/           # 상수
│   ├── controllers/         # 컨트롤러 (요청 처리)
│   ├── middleware/          # 미들웨어 (인증, 역할 확인)
│   ├── repositories/        # 리포지토리 (데이터베이스 작업)
│   ├── routes/              # API 라우트
│   ├── scripts/             # 스크립트 (마이그레이션, 동기화)
│   ├── services/            # 비즈니스 로직
│   ├── db.js               # 데이터베이스 연결
│   ├── firebaseAdmin.js    # Firebase Admin 초기화
│   └── index.js            # 서버 진입점
│
├── src/                     # 프론트엔드 애플리케이션
│   ├── api/                 # API 클라이언트
│   ├── assets/              # 정적 리소스
│   ├── auth/                # 인증 로직
│   ├── components/          # React 컴포넌트
│   │   ├── common/          # 공통 컴포넌트 (ProtectedRoute 등)
│   │   ├── layout/         # 레이아웃 컴포넌트
│   │   ├── pages/          # 페이지 컴포넌트
│   │   │   └── admin/      # 관리자 컴포넌트 (테이블)
│   │   └── ui/             # UI 컴포넌트
│   ├── config/              # 설정
│   ├── constants/           # 상수 (역할, 관리자 메뉴)
│   ├── data/                # 정적 데이터
│   ├── features/            # 기능 모듈 (FSD)
│   ├── hooks/               # 커스텀 훅 (useAdminOrder, useAdminProducts, useAdminStats)
│   ├── pages/               # 애플리케이션 페이지
│   ├── store/               # Redux store
│   ├── utils/               # 유틸리티
│   ├── App.jsx              # 메인 컴포넌트
│   └── main.jsx             # 진입점
│
├── public/                   # 공개 파일
├── package.json              # 의존성 및 스크립트
├── vite.config.js           # Vite 설정
└── tailwind.config.js       # Tailwind 설정
```

## 🔌 API

### 주요 엔드포인트:

#### 제품:
- `GET /api/products/categoriesList` - 카테고리 목록
- `GET /api/products/categories/:category` - 카테고리별 제품
- `GET /api/products/brands` - 브랜드 목록
- `GET /api/products/brands/:brand` - 브랜드별 제품
- `GET /api/products/new` - 신제품
- `GET /api/products/on-sale` - 할인 제품
- `GET /api/products/bestsellers` - 베스트셀러
- `GET /api/products/slug/:slug` - 슬러그별 제품
- `GET /api/products/search` - 제품 검색

#### 장바구니:
- `GET /api/cart` - 장바구니 가져오기
- `POST /api/cart` - 장바구니에 항목 추가
- `PUT /api/cart/:productId` - 장바구니 항목 업데이트
- `DELETE /api/cart/:productId` - 장바구니에서 항목 제거
- `DELETE /api/cart` - 장바구니 비우기
- `POST /api/cart/merge` - 게스트 장바구니를 사용자 장바구니와 병합

#### 주문:
- `POST /api/orders` - 주문 생성 (인증 필요)
- `GET /api/orders/user/:firebaseUid` - 사용자 주문
- `GET /api/orders/:orderId` - 주문 세부 정보

#### 관리자 API (특별한 역할 필요):
- `GET /api/orders/all` - 모든 주문 (admin, manager)
- `PUT /api/orders/:orderId/status` - 주문 상태 업데이트 (admin, manager)
- `GET /api/stats/popular-products` - 인기 제품 (admin 전용)
- `GET /api/products/admin/all` - 관리자용 모든 제품 (admin 전용)
- `GET /api/products/admin/:productId` - 편집용 제품 (admin 전용)
- `POST /api/products` - 제품 생성 (admin 전용)
- `PUT /api/products/:productId` - 제품 업데이트 (admin 전용)
- `PUT /api/products/:productId/active` - 활성 상태 변경 (admin 전용)

#### 사용자:
- `POST /api/users` - 사용자 생성
- `GET /api/users/me` - 현재 사용자 (인증 필요, 역할 반환)

## 💻 개발

### 아키텍처:

프로젝트는 코드 구성을 위해 **Feature-Sliced Design** 접근 방식을 사용합니다:
- `features/` - 격리된 기능 (cart, products)
- `components/` - 재사용 가능한 컴포넌트
- `pages/` - 애플리케이션 페이지
- `hooks/` - 커스텀 훅 (관리자 훅 포함: `useAdminOrder`, `useAdminProducts`, `useAdminStats`)
- `utils/` - 유틸리티
- `constants/` - 상수 (역할, 관리자 메뉴)

**컴포넌트 분리 패턴:**
관리자 페이지는 로직과 표현의 분리 패턴을 따릅니다:
- `AdminPage.jsx` (페이지) → `useAdminHook.js` (로직) + `AdminTable.jsx` (표현)

### 상태 관리:

- **Redux Toolkit** 전역 상태용
- **createEntityAdapter** 데이터 정규화용
- **createSelector** 선택자 메모이제이션용

### 스타일링:

- **Tailwind CSS** 스타일용
- 컴포넌트는 유지보수 편의를 위해 객체로 그룹화된 스타일 사용
- 반응형 디자인 (breakpoints: sm, md, lg, xl)

### 최적화:

- **지연 로딩** Intersection Observer를 통한 이미지
- **무한 스크롤** 제품 페이지네이션용
- **코드 분할** React.lazy()를 통한
- **스켈레톤 화면** UX 개선용
- 컴포넌트 및 선택자의 **메모이제이션**

### 린팅 및 포맷팅:

```bash
# 코드 확인
npm run lint

# 포맷팅
npm run format
npm run format:check
```

## 📝 스크립트

- `npm run dev` - 개발 모드에서 프론트엔드와 백엔드 실행
- `npm run dev-frontend` - 프론트엔드만 실행
- `npm run dev-backend` - 백엔드만 실행
- `npm run build` - 프로덕션용 프론트엔드 빌드
- `npm run preview` - 프로덕션 빌드 미리보기
- `npm run lint` - 코드 린팅
- `npm run format` - Prettier로 코드 포맷팅
- `npm run format:check` - 변경 없이 포맷팅 확인
- `npm run migrate` - 데이터베이스 마이그레이션 실행

## 🔒 보안

- **인증** Firebase를 통한
- **JWT 토큰** API 요청용
- **역할 기반 시스템** (user, manager, admin) 권한 확인 포함
- **역할 확인 미들웨어** 모든 관리자 엔드포인트에 대한 백엔드
- **라우트 보호** `ProtectedRoute` 컴포넌트를 통한 프론트엔드
- **CORS 설정** API 보호용
- **입력 검증** 컨트롤러 및 서비스 수준에서
- **SQL 인젝션 방지** 매개변수화된 쿼리를 통한
- **트랜잭션** 데이터 무결성 보장용

### 역할 및 권한:

- **user** (기본값) - 일반 사용자, 주문 가능
- **manager** - 주문 관리 가능 (보기, 상태 업데이트)
- **admin** - 전체 액세스: 제품 관리, 주문 관리, 통계 보기

**중요:** 실제 보호는 백엔드에서 발생합니다. 프론트엔드는 UX(인터페이스 요소 숨기기/표시)에만 사용됩니다.

## 📚 문서

자세한 문서는 `docs/` 폴더에 있습니다:

- **[auth.md](docs/auth.md)** - 인증, 권한 부여 및 역할 시스템
- **[api.md](docs/api.md)** - 완전한 API 엔드포인트 문서
- **[architecture.md](docs/architecture.md)** - 프로젝트 아키텍처
- **[database.md](docs/database.md)** - 데이터베이스 구조

## 👥 작성자

Pet project - TINT Korean Cosmetics Shop

---

**참고:** 프로젝트를 실행하려면 [설정](#설정) 섹션에 따라 모든 환경 변수와 데이터베이스를 구성해야 합니다. 관리자 패널에 액세스하려면 데이터베이스에 `admin` 또는 `manager` 역할이 있는 사용자를 생성해야 합니다.

## 🌐 다른 언어

- [English](README.md)
- [Русский (Russian)](README.ru.md)
